# 🚀 服务器部署完整指令

## 📋 部署目标
将项目部署到 **118.31.120.40**，访问 `/root/xxm_mjoys/dist/index.html`

---

## ✅ 步骤 1：上传文件到服务器

在**本地终端**执行：
```bash
cd /Users/yezewei/Documents/MySpace/WorkSpace/摸象/04 象小秘开发/xxm_mjoys
scp xxm_mjoys_dist_20260416_170946.tar.gz root@118.31.120.40:/root/xxm_mjoys/
```
**输入密码：** `yezewei123!`

---

## ✅ 步骤 2：登录服务器并解压

```bash
ssh root@118.31.120.40
```
**输入密码：** `yezewei123!`

登录后执行：
```bash
cd /root/xxm_mjoys

# 解压文件
tar -xzf xxm_mjoys_dist_20260416_170946.tar.gz

# 检查文件
ls -lh dist/

# 设置权限
chmod -R 755 dist/

# 清理发布包
rm xxm_mjoys_dist_20260416_170946.tar.gz
```

---

## ✅ 步骤 3：配置 Nginx（关键步骤）

在服务器上执行：
```bash
# 创建 Nginx 配置文件
cat > /etc/nginx/conf.d/xxm_mjoys.conf << 'EOF'
server {
    listen 80;
    server_name 118.31.120.40;
    
    # 设置根目录为你的项目 dist 目录
    root /root/xxm_mjoys/dist;
    index index.html;

    # 主应用路由 - 支持 Vue Router 的 history 模式
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|otf)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Gzip 压缩
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml;
    
    # 错误页面
    error_page 404 /index.html;
}
EOF
```

---

## ✅ 步骤 4：重启 Nginx

```bash
# 测试配置文件
nginx -t

# 重启 Nginx
systemctl restart nginx

# 检查 Nginx 状态
systemctl status nginx
```

---

## ✅ 步骤 5：验证部署

```bash
# 测试本地访问
curl -I http://localhost

# 测试外网访问
curl -I http://118.31.120.40

# 查看 Nginx 日志
tail -20 /var/log/nginx/access.log
```

---

## 🎉 完成！

现在访问：**http://118.31.120.40** 应该能看到应用了！

---

## 🔧 故障排查

### 如果页面仍然空白：

**1. 检查 Nginx 配置：**
```bash
cat /etc/nginx/conf.d/xxm_mjoys.conf
```

**2. 检查文件权限：**
```bash
ls -lh /root/xxm_mjoys/dist/
```

**3. 查看 Nginx 错误日志：**
```bash
tail -50 /var/log/nginx/error.log
```

**4. 确保 Nginx 正在运行：**
```bash
systemctl status nginx
```

**5. 检查端口监听：**
```bash
netstat -tlnp | grep :80
```

---

## 📝 重要说明

- **root 路径**: `/root/xxm_mjoys/dist`
- **index 文件**: `index.html`
- **监听端口**: `80`
- **访问地址**: `http://118.31.120.40`

---

**创建时间**: 2026-04-16
**服务器**: 118.31.120.40
**项目**: xxm_mjoys v1.0
