# 🚀 快速部署命令清单

## 当前状态
- ✅ 项目已构建：`dist/` 目录已生成 (8.3MB)
- ✅ 发布包已准备：`xxm_mjoys_dist_20260416_170946.tar.gz` (4.3MB)
- 📍 目标服务器：`root@118.31.120.40:/root/xxm_mjoys`

---

## 📝 请复制执行以下命令

### 步骤 1：上传文件到服务器

```bash
cd /Users/yezewei/Documents/MySpace/WorkSpace/摸象/04 象小秘开发/xxm_mjoys
scp xxm_mjoys_dist_20260416_170946.tar.gz root@118.31.120.40:/root/xxm_mjoys/
```
**输入密码：** `yezewei123!`

---

### 步骤 2：登录服务器并部署

```bash
ssh root@118.31.120.40
```
**输入密码：** `yezewei123!`

登录服务器后，执行以下命令：

```bash
# 进入部署目录
cd /root/xxm_mjoys

# 备份旧版本（如果存在）
if [ -d "dist" ]; then
    cp -r dist dist_backup_$(date +%Y%m%d_%H%M%S)
    echo "✅ 已备份旧版本"
fi

# 解压新版本
tar -xzf xxm_mjoys_dist_20260416_170946.tar.gz
echo "✅ 解压完成"

# 检查解压结果
ls -lh dist/

# 设置权限
chmod -R 755 dist/

# 清理发布包
rm xxm_mjoys_dist_20260416_170946.tar.gz

# 查看当前目录结构
ls -lh
```

---

### 步骤 3：检查并重启 Nginx

```bash
# 检查 Nginx 状态
systemctl status nginx

# 如果 Nginx 未运行，启动它
systemctl start nginx

# 如果 Nginx 已在运行，重启它
systemctl restart nginx

# 检查 Nginx 配置
nginx -t

# 查看 Nginx 进程
ps aux | grep nginx
```

---

### 步骤 4：验证部署

```bash
# 检查 dist 目录内容
cd /root/xxm_mjoys/dist
ls -lh

# 检查 index.html
cat index.html | head -20

# 测试本地访问
curl -I http://localhost

# 测试外网访问
curl -I http://118.31.120.40
```

---

## 🔧 故障排查

### 问题 1：页面空白或无法访问

**检查 Nginx 配置：**
```bash
cat /etc/nginx/nginx.conf
ls -la /etc/nginx/conf.d/
```

**创建 Nginx 配置（如果不存在）：**
```bash
cat > /etc/nginx/conf.d/xxm_mjoys.conf << 'EOF'
server {
    listen 80;
    server_name _;
    
    root /root/xxm_mjoys/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    gzip on;
    gzip_vary on;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;
}
EOF

# 测试配置
nginx -t

# 重启 Nginx
systemctl restart nginx
```

---

### 问题 2：资源加载失败 (404)

```bash
# 检查文件是否存在
cd /root/xxm_mjoys/dist
ls -lh assets/

# 检查文件权限
chmod -R 755 dist/

# 查看 Nginx 错误日志
tail -50 /var/log/nginx/error.log

# 查看 Nginx 访问日志
tail -50 /var/log/nginx/access.log
```

---

### 问题 3：页面显示但白屏

**检查浏览器控制台错误（F12）**

**常见问题：**
1.  basePath 配置错误
2.  资源路径错误
3.  API 接口地址错误

**解决方案：**
```bash
# 检查 index.html 内容
cat /root/xxm_mjoys/dist/index.html | head -30

# 查看是否有 basePath 配置
grep -i "base" /root/xxm_mjoys/dist/index.html
```

---

## 📊 快速诊断命令

```bash
# 一键诊断（在服务器上执行）
echo "=== Nginx 状态 ===" && systemctl status nginx --no-pager | head -5
echo ""
echo "=== 端口监听 ===" && netstat -tlnp | grep :80
echo ""
echo "=== 目录结构 ===" && ls -lh /root/xxm_mjoys/
echo ""
echo "=== dist 内容 ===" && ls -lh /root/xxm_mjoys/dist/ | head -10
echo ""
echo "=== 测试访问 ===" && curl -I http://localhost 2>&1 | head -5
```

---

## 🆘 回滚命令（如果需要）

```bash
cd /root/xxm_mjoys

# 列出备份
ls -la | grep dist_backup

# 恢复最近的备份
rm -rf dist
mv dist_backup_YYYYMMDD_HHMMSS dist
chmod -R 755 dist/
systemctl restart nginx
```

---

## ✅ 成功标志

完成部署后，访问 **http://118.31.120.40** 应该能看到应用正常加载。

**验证清单：**
- [ ] Nginx 正在运行
- [ ] 80 端口已监听
- [ ] dist/index.html 存在
- [ ] dist/assets/ 目录有 JS/CSS 文件
- [ ] curl http://localhost 返回 200 状态码
- [ ] 浏览器访问无错误

---

**创建时间：** 2026-04-16 17:20
**项目版本：** xxm_mjoys v1.0
